import { DisplayDateclsPipe } from './display-datecls-pipe';

describe('DisplayDateclsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayDateclsPipe();
    expect(pipe).toBeTruthy();
  });
});
